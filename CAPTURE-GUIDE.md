# Media to capture for the marketing site

Drop files into `website/assets/` (shared with the main site), then tell the agent to wire them in.

## Yes — screen record these (30–60 sec each)

Use **Windows + G** (Xbox Game Bar) or OBS. Record at **1920×1080**, the Fund Pilot window maximized, dark theme on.

| Clip | Show this flow |
|------|----------------|
| `demo-analyze.mp4` | Drop PDFs → parse → scoring + funding range + weekly positions |
| `demo-pilot.mp4` | Open Pilot → voice/text trace → owner cell result → click to CRM/dialer |
| `demo-dialer.mp4` | Open campaign → click-to-call → disposition saved on deal |
| `demo-crm.mp4` | Deal board → open merchant → activity / pipeline filter |
| `demo-leads.mp4` | Lead Hunter trace → admin CSV assignment → broker distribution controls |
| `demo-showreel.mp4` | Optional: your best 45-sec cut of all of the above |

Keep mouse movement slow and deliberate — marketing, not speed-run.

## Also grab static PNG screenshots

Full window, no personal/merchant PII (blur or use demo merchants).

| File | Screen |
|------|--------|
| `desktop-analyze-summary.png` | Analyze tab with score + range visible |
| `desktop-analyze-positions.png` | Weekly positions / stacking panel |
| `desktop-crm-board.png` | CRM deal board (desktop) |
| `desktop-crm-deal.png` | Single deal detail |
| `desktop-pilot.png` | Pilot panel open with a trace result |
| `desktop-dialer.png` | Campaign queue / active call UI |
| `desktop-leads.png` | Lead marketplace (when ready) |

Mobile CRM shots (phone mockups on the site): export from mobile web or resize desktop — `iphone-app-crm.png`, etc.

## What we'll do with them

- **Videos** → cinema monitor + laptop bezel in scroll sections (Apple-style motion on real UI)
- **PNG** → swap per scroll step when you scroll (analyze step 1 = video, step 2 = positions screenshot, etc.)
- **Hero** → laptop + phone device frames with your best analyze clip

No random 3D objects for product — only gold ambient background behind your real screens.
